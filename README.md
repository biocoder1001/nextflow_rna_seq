# nextflow_rna_seq

This is a nextflow pipeline for the rna seq analysis, it currently runs on the slurm cluster. It takes the fastq files, does all the 
requuired preprocessing, followed by only taking the samples that are required for the differential expression analysis and further 
downstream PCA analysis. 

